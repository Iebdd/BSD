import java.util.Scanner;

public class Encryption {
    private enum menu {QUIT, ENCRYPT, DECRYPT};
    public static void main(String[] args) {
        int command = 0;
        while(true) {
            command = Reader.readInt();
        }

    }

    private menu parseCommand(menu command) {
        
    }

}
